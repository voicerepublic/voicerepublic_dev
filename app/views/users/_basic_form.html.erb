<%# dashboard/settings_edit %>

<%= form_for user, :url => user_path(:id => user, :url_arg => 1), :html => { :class => 'form-horizontal' } do |f| %>
  
  <div class="control-group">
    <%= f.label "Email-Adresse bearbeiten", :for => "user_email", :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :email, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit t(".Save Email", :default => "Email-Adresse speichern"), :class => 'btn btn-primary' %>
  </div>
<% end %>
