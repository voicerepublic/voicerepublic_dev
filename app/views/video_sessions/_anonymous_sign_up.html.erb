You can't call a charged KluuU as Guest.....do you want to sign up?

<%= button_to t('.sign_up', :default => 'sign up'), new_user_registration_path(), :method => :get, :class => 'btn' %>
or
<%= link_to t('.close'), '#', :data_function => 'closeOverlay' %>
