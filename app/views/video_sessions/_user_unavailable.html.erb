User unavailable at the moment.....do you want to send a message?

<%= button_to t('.send_message', :default => 'send message'), new_message_path(:receiver_id => receiver_id), :method => :get, :class => 'btn' %>
or
<%= link_to t('.close'), '#', :id => 'close_overlay_link' %>
