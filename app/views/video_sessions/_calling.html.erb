Calling in process.....
<%= button_to t('.cancel_call', :default => 'cancel call'), video_session_path(:id => video_session.id, :canceling_participant_id => video_session.guest_participant.id), :method => :delete, :remote => :true, :class => 'btn' %>