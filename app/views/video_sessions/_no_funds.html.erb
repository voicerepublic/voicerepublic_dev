You don't have enough credit.....do you want to check in some money?

<%= button_to t('.check_in_money', :default => 'check in money'), new_user_balance_check_in_order_path(:user_id => current_user.id), :method => :get, :class => 'btn' %>
or
<%= link_to t('.close'), '#', :data_function => 'closeOverlay' %>
