%ul.dropdown-menu
  %li.menu-item
    = link_to user_path(:id => guest_or_current_user) do
      = t('.my_profile')
  %li.menu-item
    = link_to( new_venue_path ) do
      %span.label.label-venue= t('.create_venue')
  %li.menu-item
    = link_to user_conversations_path(:user_id => guest_or_current_user), :title => "#{t('.messages')}" do
      = t('.messages')
  %li.menu-item
    = link_to(dashboard_contacts_path) do
      = t('.contacts')
  %li.menu-item
    = link_to dashboard_settings_edit_path do
      = t('.settings')
