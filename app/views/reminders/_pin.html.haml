- target = [ rememberable, :reminders ]
= link_to target, method: 'post', :data => { :msg => 'pinned' }, class: "button pin-btn", title: t('.pin'), remote: true, 'data-ga-event' => "click #{gacategory} pin talk:#{rememberable.id}" do
  = naked_icon('pin')
  %span.pin-msg= t('.pin')
