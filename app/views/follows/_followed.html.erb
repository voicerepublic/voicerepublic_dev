<li class="followed">
	 
	<div class="thumbnail user-thumbnail">
		
		<%# LINK FOR WHOLE THUMBAIL %>
		<%= link_to user_path(:id => followed_relation.followed), :class => "user-thumbnail-link clearfix" do %>	
			<%= image_tag(followed_relation.followed.account.portrait.url(:thumb)) %>
			<%= followed_relation.followed.name %>
		<% end %>
			
		<%= link_to user_follow_path(:user_id => @user, :id => followed_relation), :title => "Delete", :class => "close follows-user-thumbnail-unfollow", :method => :delete do %>
			&times;
		<% end %>

	</div>
	
</li>
