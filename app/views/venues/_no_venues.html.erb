<p><%= t('.create_venue_for_your_kluuu') %></p>
<br>
<div class="control-group">
	<%= link_to( t('.create_venue'), new_venue_path, :class => "btn btn-primary") if @user == guest_or_current_user %>
</div>
