<p><%= t('.these_are_your_venues', :name => @user.firstname ) %> ... <strong>to be told in case you're blind</strong></p>

<div class="control-group">
	<%= link_to( t('.create_venue'), new_venue_path, :class => "btn btn-primary") if @user == current_user %>
</div>
