<!DOCTYPE html>
<html lang="<%= I18n.locale.to_s %>" xmlns:fb="http://ogp.me/ns/fb#" ng-app="sencha">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= content_for?(:facebook_values) ? yield(:facebook_values) : '' %>
    <title><%= content_for?(:title) ? yield(:title) : t(".landing_title") %> </title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <link href="//cloud.webtype.com/css/fa41b746-9201-4f1c-ab4a-a330eb0afb96.css" rel="stylesheet" type="text/css" />
    <%= stylesheet_link_tag "application", media: "all" %>
    <%= favicon_link_tag '/favicon.ico' %>
    <link href="/images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="/images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="/images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
    <link href="//cloud.webtype.com/css/fa41b746-9201-4f1c-ab4a-a330eb0afb96.css" rel="stylesheet" type="text/css" />
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:400,300,700,600' rel='stylesheet' type='text/css'>
    <script src="//use.typekit.net/ilh7iri.js"></script>
    <script>try{Typekit.load();}catch(e){}</script>
    <%= yield(:head) %>
    <%= javascript_include_tag "vendor/modernizr" %>

  </head>
  <body class="<%= [controller_name, action_name] * '-' %>">
    <%= render "shared/how_to_use_vr" %>
    <%= render "shared/actionbar" %>
    <%= render "shared/unconfirmed" if @current_user && !@current_user.confirmed? %>
    <%= render "shared/notice_or_error" %>

    <div ng-controller='NagUsersController'>
      <script>
        current_user = {}
        current_user.is_anonymous = <%= !user_signed_in? %>
        current_user.id = <%= user_signed_in? ? current_user.id : 0 %>
      </script>
      <div id="nagModal" class="reveal-modal nag-modal" data-reveal aria-labelledby="modalTitle" aria-hidden="true" role="dialog">
        <div class="row">
          <div id="logobox" class="small-12 large-4 small-centered large-uncentered columns">
            <img src="/images/logos/vr_red.svg">
          </div>
          <div class="small-12 large-8 small-centered large-uncentered columns">


            <div class="nagBody">
              <h2 id="modalTitle"><%= t('nag_modal.title') %></h2>
              <h4 class="lead"><%= t('nag_modal.lead').html_safe %></h4>
              <%= t('nag_modal.body').html_safe %>
            </div>
          </div>
          <a class="close-reveal-modal">&#215;</a>
        </div>
      </div>
    </div>

    <%= yield %>

    <%= render "shared/footer" %>
    <div id='scripts'>
      <%= javascript_include_tag "application" %>
      <% if Rails.env.production? %>
        <%= render 'shared/google_analytics' unless @current_user && @current_user.insider? %>
        <%= render 'shared/tawk' %>
      <% end %>
      <%= yield :javascript %>
    </div>
  </body>
</html>
