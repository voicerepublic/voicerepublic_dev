<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0, minimum-scale=1.0, maximum-scale=1.0">
		<title><%= content_for?(:title) ? yield(:title) : "Kluuu2" %></title>
		<%= csrf_meta_tags %>
		<!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
		<!--[if lt IE 9]>
		<script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
		<![endif]-->
		<%= stylesheet_link_tag controller.controller_name, :media => "all" %>
		<%= javascript_include_tag "private_pub" %>
		<link href="/images/favicon.ico" rel="shortcut icon">
		<link href="images/apple-touch-icon.png" rel="apple-touch-icon">
		<link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
		<link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">

	</head>
	<body>
		<% if current_user %>
		<%= render(:partial => "shared/actionbar") %>
		<% else %>
		<%= render(:partial => "shared/navbar")%>
		<% end %>
		<%= render(:partial => "shared/notice_or_error") %>

		<div class="container">
			
				<div class="user-container">
					<section class="user-information well">
						<header>
							<%= link_to(image_tag( @user.account.portrait.url(:large), :class => "img-polaroid"), user_path( :id => @user), :class=> "user-image") %>
							<h2 class="user-name"><%= link_to(@user.name, user_path( :id => @user)) %></h2>
							<div class="user-activity">
								<i class="icon-bolt"></i> 55
							</div>
							<div class="user-about"><%= simple_format(@user.account.about) %></div>
						</header>
						<%= render(:partial => 'users/user_information', :locals => { :user =>  @user } ) %>
						<ul class="user-nav unstyled">
							<li>
								<%= link_to(new_message_path(:receiver_id => @user.id), :class => "btn" ) do %>
								<i class="icon-envelope"></i> <%= t('.send_message') %>
								<% end unless current_user && current_user.id == @user.id %>
							</li>
							<li>
								<% if current_user && current_user.id != @user.id %>
									<% if current_user.followed.include?(@user) %>
										<%= link_to(t('.unfollow'), user_follow_path(:user_id => current_user, :id => current_user.followed_relations.where(:followed_id => @user).first), :class => "btn", :method => :delete) %>
									<% else %>
										<%= link_to( t('.follow'), user_create_follow_path(:user_id => current_user, :followed_id => @user ), :method => "post", :class => "btn btn-small" )%>
									<% end %>
								<% end %>
							</li>
						</ul>
					</section>
						<div class="user-content ">
							<%= render(:partial => 'users/user_content_nav', :locals => { :user => @user } ) %>

							<section class="user-content-shown clearfix">
								<%= yield %>
							</section>
						</div>

					
				</div>
				<footer>
					<p>
						&copy; KluuU 2012
						<br />
						<small><span class="label">rev: <%= git_revision %> - version <%= KluuuCode::VERSION %></span></small>
					</p>
				</footer>
			</div>
		</div>
		<!-- /container -->

		<!-- Javascripts
		================================================== -->
		<!-- Placed at the end of the document so the pages load faster -->
		<%= javascript_include_tag "application" %>
		<%= javascript_include_tag "private_pub" %>
		<%= subscribe_to "/notifications/new" %>
	</body>
</html>